programa ejemplo
variables a,b,c : entero;
variables e,d : real;
(* esto es un comentario *)
procedimiento sumar (aux,y: in entero; resul: in out entero)
variables aux:entero;
{
aux=resul;
resul=y;
repetir {
aux = aux - 1;
resul = resul+1;
 } hasta aux == 0 ;
} 

{
leer(a); leer(b);
d= 1.0/e;
e= 1.0/d;
b= a*(c*b)+a;
escribir_linea(c*c);

}